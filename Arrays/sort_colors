class Solution {
    public void sortColors(int[] nums) {
        int n=nums.length;
        for(int i=0;i<n;i++){
            Arrays.sort(nums);
        }
        for(int i=0;i<n;i++){
          System.out.println(nums[i]);
        }
        
        
    }
}
